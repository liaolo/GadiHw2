@using MySql.Data.MySqlClient;
@page "/AssigmentDelete"

<input type="password" @bind="userid" placeholder="Enter you userid" />
<button @onclick="DELETE">sumbit</button>
<p>@Message</p>


@code
{
    string userid = "";
    string Message = "";

    void DELETE()
    {
        MySqlConnection con = new MySqlConnection();
        con.ConnectionString = "server=localhost;user id =root;password=josh17rog;database=gadhw1";
        con.Open();

        string Sql2 = $"DELETE FROM `gadhw1`.`userdatabase` WHERE(`IdUsers` = '{userid}');";
        MySqlCommand cmd2 = new MySqlCommand();
        cmd2.Connection = con;
        cmd2.CommandText = Sql2;
        int num = cmd2.ExecuteNonQuery();
        userid = num > 0 ? "Succesful" : "No No";
    }
}

<style>
    /* Page theme scoped via :has() so it won't affect other pages */
    body:has(input[type="password"][placeholder="Enter you userid"]) {
      --bg1: #0b1020;
      --bg2: #120a2a;
      --cardBorder: rgba(255, 255, 255, 0.14);
      --text: rgba(255, 255, 255, 0.92);
      --muted: rgba(255, 255, 255, 0.65);
      --radius: 18px;

      min-height: 100vh;
      margin: 0;
      display: grid;
      place-items: center;
      padding: 56px 16px;

      color: var(--text);
      font-family: ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, Arial;
      background:
        radial-gradient(900px 520px at 20% 15%, rgba(120, 80, 255, 0.20), transparent 60%),
        radial-gradient(700px 420px at 85% 25%, rgba(0, 220, 255, 0.16), transparent 55%),
        linear-gradient(160deg, var(--bg1), var(--bg2));
    }

    body:has(input[type="password"][placeholder="Enter you userid"]) * { box-sizing: border-box; }

    body:has(input[type="password"][placeholder="Enter you userid"]) input,
    body:has(input[type="password"][placeholder="Enter you userid"]) button,
    body:has(input[type="password"][placeholder="Enter you userid"]) p {
      width: min(460px, 92vw);
    }

    /* Glass card (using p as the "status pill" and backdrop via body pseudo element) */
    body:has(input[type="password"][placeholder="Enter you userid"])::before {
      content: "DELETE USER";
      position: fixed;
      top: 32px;
      left: 50%;
      transform: translateX(-50%);
      letter-spacing: 0.28em;
      font-weight: 800;
      font-size: 14px;
      color: rgba(255, 255, 255, 0.72);
      text-shadow: 0 0 22px rgba(120, 80, 255, 0.45);
      z-index: 2;
    }

    body:has(input[type="password"][placeholder="Enter you userid"])::after {
      content: "";
      position: fixed;
      inset: 0;
      display: grid;
      place-items: center;
      z-index: 0;
      pointer-events: none;
      background:
        radial-gradient(520px 320px at 50% 50%,
          rgba(255,255,255,0.10),
          rgba(255,255,255,0.05),
          transparent 70%);
    }

    body:has(input[type="password"][placeholder="Enter you userid"]) input,
    body:has(input[type="password"][placeholder="Enter you userid"]) button,
    body:has(input[type="password"][placeholder="Enter you userid"]) p {
      position: relative;
      z-index: 1;
    }

    body:has(input[type="password"][placeholder="Enter you userid"]) input {
      padding: 14px 14px;
      border-radius: var(--radius);
      border: 1px solid rgba(255, 255, 255, 0.14);
      background: rgba(15, 18, 35, 0.55);
      color: var(--text);
      outline: none;
      box-shadow: 0 10px 25px rgba(0,0,0,0.25);
      transition: transform 160ms ease, border-color 160ms ease, box-shadow 160ms ease;
    }

    body:has(input[type="password"][placeholder="Enter you userid"]) input::placeholder {
      color: rgba(255, 255, 255, 0.45);
    }

    body:has(input[type="password"][placeholder="Enter you userid"]) input:focus {
      border-color: rgba(0, 220, 255, 0.55);
      box-shadow: 0 0 0 3px rgba(0, 220, 255, 0.16), 0 14px 35px rgba(0,0,0,0.35);
      transform: translateY(-1px);
    }

    body:has(input[type="password"][placeholder="Enter you userid"]) button {
      padding: 12px 14px;
      border-radius: var(--radius);
      border: 1px solid rgba(255,255,255,0.18);
      cursor: pointer;
      font-weight: 800;
      letter-spacing: 0.08em;
      color: rgba(255,255,255,0.92);
      background: linear-gradient(135deg, rgba(255, 70, 120, 0.95), rgba(120, 80, 255, 0.90));
      box-shadow: 0 14px 34px rgba(255, 70, 120, 0.18), 0 18px 46px rgba(120, 80, 255, 0.18);
      transition: transform 140ms ease, filter 140ms ease;
    }

    body:has(input[type="password"][placeholder="Enter you userid"]) button:hover { transform: translateY(-2px); filter: brightness(1.05); }
    body:has(input[type="password"][placeholder="Enter you userid"]) button:active { transform: translateY(0px) scale(0.99); }

    body:has(input[type="password"][placeholder="Enter you userid"]) p {
      margin: 10px 0 0;
      padding: 10px 12px;
      border-radius: 999px;
      border: 1px solid rgba(255,255,255,0.12);
      background: rgba(0,0,0,0.25);
      color: var(--muted);
      text-align: center;
      overflow-wrap: anywhere;
    }
</style>
